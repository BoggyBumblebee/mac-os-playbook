[defaults]
nocows = True
roles_path = ./roles:/etc/ansible/roles
inventory = inventory
become = True
callback_result_format = yaml

# Galaxy roles in this playbook still use top-level Ansible facts such as
# ansible_distribution. Keep the current behavior explicit until those roles are
# updated to read from ansible_facts.
inject_facts_as_vars = True
